- name: Setup Android SDK
  uses: android-actions/setup-android@v3
  with:
    # Do NOT install the removed 'tools' package
    packages: 'platform-tools'   # or whatever you actually need
    # Optional but recommended – pin a known-good cmdline-tools revision
    cmdline-tools-version: 12266719   # the one the action already downloaded